<Window x:Class="Tool_SqlPerformanceAnalyzer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Tool_SqlPerformanceAnalyzer"
        mc:Ignorable="d"
        Title="SQL 效能分析工具" Height="700" Width="1200"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 連線設定區域 -->
        <GroupBox Grid.Row="0" Header="資料庫連線設定" Margin="10">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Label Grid.Row="0" Grid.Column="0" Content="伺服器:"/>
                <ComboBox Grid.Row="0" Grid.Column="1" Name="cmbServer" Margin="5,0" IsEditable="True" 
                          SelectionChanged="cmbServer_SelectionChanged">
                    <ComboBoxItem Content="192.17.2.01"/>
                    <ComboBoxItem Content="192.17.2.02"/>
                </ComboBox>

                <Label Grid.Row="0" Grid.Column="2" Content="資料庫:" Margin="10,0,0,0"/>
                <ComboBox Grid.Row="0" Grid.Column="3" Name="cmbDatabase" Margin="5,0" IsEditable="True"/>

                <Button Grid.Row="0" Grid.Column="4" Name="btnRefreshDB" Content="重新整理" Width="70" Height="25" Margin="5,0" Click="btnRefreshDB_Click"/>

                <RadioButton Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Name="rbWindows" Content="Windows 驗證" IsChecked="True" Margin="0,10,0,0"/>
                <RadioButton Grid.Row="1" Grid.Column="2" Grid.ColumnSpan="2" Name="rbSql" Content="SQL Server 驗證" Margin="10,10,0,0"/>

                <Label Grid.Row="2" Grid.Column="0" Content="使用者名稱:"/>
                <TextBox Grid.Row="2" Grid.Column="1" Name="txtUsername" Margin="5,5,5,0" IsEnabled="{Binding IsChecked, ElementName=rbSql}"/>

                <Label Grid.Row="2" Grid.Column="2" Content="密碼:" Margin="10,0,0,0"/>
                <PasswordBox Grid.Row="2" Grid.Column="3" Name="txtPassword" Margin="5,5,5,0" IsEnabled="{Binding IsChecked, ElementName=rbSql}"/>

                <Button Grid.Row="2" Grid.Column="5" Name="btnTest" Content="測試連線" Width="80" Height="25" Margin="10,5,0,0" Click="btnTest_Click"/>
            </Grid>
        </GroupBox>

        <!-- 查詢選擇區域 -->
        <GroupBox Grid.Row="1" Header="查詢選擇" Margin="10,0,10,10">
            <StackPanel Orientation="Horizontal" Margin="10">
                <Button Name="btnQuery1" Content="IO 耗用最大的 SP" Width="150" Height="30" Margin="5" Click="btnQuery1_Click"/>
                <Button Name="btnQuery2" Content="平均每秒執行次數" Width="150" Height="30" Margin="5" Click="btnQuery2_Click"/>
                <Button Name="btnQuery3" Content="執行次數排序" Width="150" Height="30" Margin="5" Click="btnQuery3_Click"/>
                <Button Name="btnSave" Content="儲存連線設定" Width="100" Height="30" Margin="20,5,5,5" Click="btnSave_Click"/>
                <Button Name="btnLoad" Content="載入連線設定" Width="100" Height="30" Margin="5" Click="btnLoad_Click"/>
            </StackPanel>
        </GroupBox>

        <!-- 結果顯示區域 -->
        <GroupBox Grid.Row="2" Header="查詢結果" Margin="10,0,10,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Name="txtStatus" Margin="10,5" FontWeight="Bold" Foreground="Blue"/>
                <DataGrid Grid.Row="1" Name="dgResults" Margin="10" AutoGenerateColumns="True" 
                         IsReadOnly="True" CanUserAddRows="False" CanUserDeleteRows="False"
                         AlternatingRowBackground="LightGray" 
                         GridLinesVisibility="Horizontal"
                         HeadersVisibility="Column"
                         CanUserResizeColumns="True"
                         CanUserSortColumns="True"
                         SelectionMode="Single"
                         SelectionUnit="FullRow" >
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="DarkBlue"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="8,6"/>
                            <Setter Property="BorderBrush" Value="White"/>
                            <Setter Property="BorderThickness" Value="0,0,1,0"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="White"/>
                            <Setter Property="Height" Value="28"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="LightBlue"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="CornflowerBlue"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>
                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="8,4"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.CellStyle>
                    <DataGrid.Resources>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                            <Setter Property="VerticalAlignment" Value="Top"/>
                        </Style>
                    </DataGrid.Resources>
                </DataGrid>
            </Grid>
        </GroupBox>
    </Grid>
</Window>
